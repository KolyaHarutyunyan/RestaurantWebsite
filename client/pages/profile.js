import { ProfilePage } from "@eachbase/pages";
import { Layout } from "@eachbase/fragments";

const Profile = () => {
  return (
    <Layout>
      <ProfilePage />
    </Layout>
  );
};

export default Profile;
