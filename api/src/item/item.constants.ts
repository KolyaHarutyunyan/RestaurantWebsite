export const summaries = {
  ADD_IMAGE: `Adds an image to the images of the array if the maximum (6) is not reached including the main image.
                 If the item does not have a main image it adds this image as the main image. 
                 If it does have a main image, first checks if the max is reached (5, not counting main image), and adds to the images`,
  CHANGE_MAIN: `Swaps the main image with the image provided. This means the old main image is now in the images and the image with the
                image id from the images is now the main image and removed from the images`,
};
